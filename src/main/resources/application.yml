spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:scholarhaven}
    username: ${DB_USERNAME:zunaied}
    password: ${DB_PASSWORD:zunaied}

  # Java Persistence API - Works with relational databases using Java objects instead of raw SQL
  jpa:
    # Convert object into SQL
    hibernate:
      # Update schema if needed when the app starts
      ddl-auto: validate
    # Print all generated SQL queries in the console for debugging
    show-sql: true

# Replace the contents later
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000